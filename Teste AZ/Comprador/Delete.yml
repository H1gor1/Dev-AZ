info:
  name: Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:8080/comprador
  params:
    - name: empresaId
      value: "1"
      type: query
    - name: leilaoId
      value: "5"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
