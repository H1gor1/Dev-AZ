info:
  name: Create
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/comprador
  body:
    type: json
    data: |-
      {
        "empresaId": 1,
        "leilaoId": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
