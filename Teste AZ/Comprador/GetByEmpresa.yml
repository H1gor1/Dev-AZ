info:
  name: GetByEmpresa
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/comprador/empresa/:empresaId
  params:
    - name: empresaId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
