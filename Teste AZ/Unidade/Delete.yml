info:
  name: Delete
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:8080/unidade/:id
  params:
    - name: id
      value: "11"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
