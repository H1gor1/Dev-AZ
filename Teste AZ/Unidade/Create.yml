info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/unidade
  body:
    type: json
    data: |-
      {
        "nome": "Dúzia"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
