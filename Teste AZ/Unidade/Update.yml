info:
  name: Update
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/unidades/:id
  params:
    - name: id
      value: "11"
      type: path
  body:
    type: json
    data: |-
      {
        "nome": "Dúzia (atualizado)"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
